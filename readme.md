# Logo Vectorizer
A self-hosted tool to convert images (JPG/PNG) into CAD-ready DXF files for Onshape.

## Usage
1. Copy the docker-compose.yml file.
2. Run `docker compose up -d`.
3. Open browser to http://localhost:8501